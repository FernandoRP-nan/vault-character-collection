# Contribuir

1. Fork → clona fuera del vault → `npm install` → `npm run build`.
2. PRs acotados; sin archivos `.db` ni imágenes con copyright ajeno sin permiso.

La ruta de imágenes se lee del frontmatter de una nota de configuración en el vault del usuario.

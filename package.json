{
  "name": "vault-character-collection",
  "version": "2.2.0",
  "description": "Visual character collection with tiers, tags, and configurable local image paths.",
  "main": "main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/FernandoRP-nan/vault-character-collection.git"
  },
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.15.17",
    "esbuild": "0.25.5",
    "obsidian": "latest",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "sql.js": "^1.14.1"
  }
}
